header:
  version: 23
  includes:
    - yocto/kas/base.yml

target:
  - logistics-input-image


local_conf_header:
  input-vedauart: |
    RPI_EXTRA_CONFIG:append = "\n# Enable VEDAUART\ndtoverlay=vedauart\n"


  production-security: |
    EXTRA_IMAGE_FEATURES += "allow-root-login"

  production-identity: |
    hostname:pn-base-files = "input-node-01"
